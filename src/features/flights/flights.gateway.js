// const baseUrl = 'https://api.iev.aero/api/flights';

// export const fetchFlightsData = async date => {
//   const response = await fetch(`${baseUrl}/${date}`);
//   if (!response.ok) {
//     throw new Error('error');
//   }

//   return response.json();
// };
